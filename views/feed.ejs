<% include ./partials/header.ejs %>

<div class="bg-gray-900 px-7 py-2 text-white w-full min-h-screen">
    <div class="cards flex flex-wrap gap-5">
        <% pins.forEach(function(elem){ %>
            <div class="card w-24">
                <div class="w-full h-32 rounded-lg overflow-hidden">
                    <img src="/images/uploads/<%=elem.image%>" alt="" class="object-cover w-full h-full ">
                </div>
                <a href="#" class="mt-2 text-xs font-semibold"><%= elem.title %> </a>
                <a href="#" class="mt-2 text-xs font-semibold opacity-50"><%= elem.user.username%> </a>
            </div>
        <% }) %>
    </div>
</div>

<% include ./partials/footer.ejs %>
